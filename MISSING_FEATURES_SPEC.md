# Craftisle Draw — 缺失功能规格 (User Perspective)

> 基于实际 Safari 用户体验分析，按优先级排列

---

## 🔴 P0 — 必须有（没有就没法正常用）

### 1. 修复 "Invalid Date" Bug

**当前状态：** 首页白板列表全部显示 "Updated Invalid Date"

**根因：** API 返回 `updated_at`（snake_case），BoardList 组件读 `board.updatedAt`（camelCase），字段名不匹配

**修复：**
- `src/lib/boards.ts` 的 `getUserBoards()` 返回时映射为 camelCase
- 或 BoardList 改为读 `board.updated_at`
- 同时检查 `created_at` 同理

---

### 2. 重命名白板

**当前状态：** 所有白板都是 "Untitled Board"，用户无法重命名

**用户场景：**
- 首页列表里直接双击/点击标题编辑
- 白板编辑器里点击顶部标题编辑
- 重命名后自动保存

**实现要点：**
- API: `PUT /api/boards/[id]` 已支持 `title` 字段 ✅
- 需要 UI：首页列表 inline edit；编辑器内标题编辑
- `BoardClient.tsx` 的 `handleSave` 传入 `title` 或单独调用 rename API

---

### 3. 分享功能完善

**当前状态：** 首页有 "Share" 链接指向 `/share/[id]`，但：
- 没有 UI 控制白板是否公开/私有
- `is_public` 字段存在但无设置入口
- 分享页是只读，无法协作编辑

**用户场景：**
- 首页 → 点击 Share → 弹出分享设置（公开/私有、复制链接）
- 开启公开后，任何人持链接可查看
- （未来）开启协作后，多人同时编辑

**实现要点：**
- 首页每个白板卡片加分享设置弹窗
- `PUT /api/boards/[id]` 已支持 `isPublic` ✅
- 分享页需要"复制链接"按钮

---

### 4. 导出格式扩展

**当前状态：** 只有右上角 "Export PNG" 按钮（客户端截图导出）

**缺失格式：**
- **SVG** — 矢量格式，无损缩放（Excalidraw 原生支持）
- **JSON** — 原始数据，可重新导入编辑
- **PDF** — 方便打印和文档嵌入

**实现要点：**
- Excalidraw API 支持 `exportToBlob({ mimeType: "image/svg+xml" })`
- JSON 导出直接 `JSON.stringify(elements)`
- 导出菜单做成下拉，替代单个 Export PNG 按钮

---

### 5. 白板缩略图（首页预览）

**当前状态：** 首页只有标题 + 日期，没有视觉预览

**用户价值：** 用户靠缩略图快速识别白板内容，而不是靠记标题

**实现方案（两种）：**
1. **静态缩略图** — 首次保存/每次保存后服务端渲染 PNG 存到 storage，列表页展示
2. **纯 CSS 预览** — 用 Excalidraw 的 `exportToBlob` 在客户端生成缩略图，存到 IndexedDB 或 Base64 存 DB

**推荐 P0 方案：** 客户端生成缩略图 Base64 存到 `boards.thumbnail` 字段，列表渲染

---

## 🟡 P1 — 应该有（显著提升体验）

### 6. 搜索和筛选白板

**当前状态：** 白板多了以后找不到

**功能：**
- 按标题搜索
- 按公开/私有筛选
- 按更新时间排序（新版/旧版/名称）

---

### 7. 复制白板

**当前状态：** 没有"复制"功能，想复用内容只能重新画

**功能：** 首页白板卡片 hover 出现"复制"按钮，调用 `POST /api/boards/duplicate`

---

### 8. 白板排序

**当前状态：** 固定按 `updated_at DESC` 排序，用户无法调整

**功能：**
- 按名称 A-Z
- 按创建时间
- 按更新时间
- 手动拖拽排序（远期）

---

### 9. 模板库

**当前状态：** 每次新建都是完全空白画布

**功能：**
- 内置 5-10 个常用模板（空白、思维导图、用户旅程地图、架构图、时间线等）
- 新建白板时先选模板

---

### 10. 实时协作（多人同时编辑）

**当前状态：** 无协作，分享页只读

**功能：**
- 基于 Excalidraw 的 WebSocket 协作协议
- 显示在线用户头像/光标
- 需要 WebSocket 服务端（Pusher/Ably/Socket.io）

**难度：** 高，建议 P2 或单独排期

---

## 🔵 P2 — 未来考虑

### 11. 文件夹/标签组织
### 12. 评论和标注
### 13. 离线编辑（Service Worker + 同步）
### 14. 移动端适配
### 15. 快捷键帮助面板
### 16. 白板版本历史（回滚）
### 17. 批量操作（多选删除）
### 18. 用户设置和头像上传

---

## 优先级总结

| 优先级 | 功能 | 工作量 | 用户影响 |
|--------|------|--------|---------|
| P0 🔴 | 修复 Invalid Date bug | 小 | 高 — 当前完全不可用 |
| P0 🔴 | 重命名白板 | 中 | 高 — 基本可用性 |
| P0 🔴 | 分享设置 UI | 中 | 高 — 核心功能 |
| P0 🔴 | 导出格式扩展 | 中 | 高 — 用户高频需求 |
| P0 🔴 | 白板缩略图 | 大 | 中 — 体验提升明显 |
| P1 🟡 | 搜索/筛选 | 小 | 中 — 白板多了必须有 |
| P1 🟡 | 复制白板 | 小 | 中 |
| P1 🟡 | 排序选项 | 小 | 低 |
| P1 🟡 | 模板库 | 中 | 中 |
| P2 🔵 | 实时协作 | 大 | 高 — 但技术难度高 |

---

## 建议排期

**Sprint 1（本周）:**
1. 修复 Invalid Date bug
2. 重命名白板（首页 + 编辑器内）
3. 分享设置 UI（公开/私有切换 + 复制链接）
4. 导出格式扩展（SVG + JSON）

**Sprint 2:**
5. 白板缩略图
6. 搜索/筛选
7. 复制白板
8. 排序选项

**Sprint 3:**
9. 模板库
10. 实时协作（调研 + POC）

