# Autoplan Review Report — craftisle-draw

> Auto-generated by `/autoplan` skill on 2026-06-13.
> Analyzes full codebase: security, performance, accessibility, code quality.

---

## Executive summary

| Category | Score | Trend |
|----------|-------|--------|
| Security | 7/10 | ⚠️ Missing CSRF protection, env var exposure risk |
| Performance | 8/10 | ✅ Prisma optimize, Excalidraw lazy load |
| Accessibility | 7/10 | ⚠️ ARIA role mismatch (D7 from design-review) |
| Code quality | 8/10 | ✅ Consistent Typescript, good error handling |
| Deploy readiness | 9/10 | ✅ Vercel auto-deploy, build passes |

**Overall recommendation:** ✅ **Ship** — address high-priority security items first.

---

## Security review

### 🔴 High — `NEXTAUTH_SECRET` must be strong
**File:** `.env.local` (user-provided)
**Issue:** If `NEXTAUTH_SECRET` is weak, session hijacking is possible.
**Check:** Ensure `NEXTAUTH_SECRET` is 32+ chars (generate with `openssl rand -base64 32`).
**Status:** ⚠️ Verify in deployment environment.

### 🔴 High — No CSRF protection on API routes
**Files:** `src/app/api/boards/route.ts`, `src/app/api/boards/[id]/route.ts`
**Issue:** NextAuth v4 protects `/api/auth/*` but custom API routes (`/api/boards`) don't have explicit CSRF checks.
**Recommendation:** Add CSRF token validation or use `getServerSession()` to verify auth on every API route.
**Status:** ⚠️ Needs fix.

### 🟡 Medium — `DATABASE_URL` in environment
**File:** `.env.local`
**Issue:** `DATABASE_URL` contains credentials (password in connection string).
**Recommendation:** Ensure `.env.local` is in `.gitignore` (it is by default with Next.js). Verify no commit leaks credentials.
**Status:** ✅ `.env.local` is not tracked (verified).

### 🟡 Medium — No rate limiting on API routes
**Files:** `src/app/api/boards/route.ts`
**Issue:** API routes have no rate limiting — susceptible to DoS.
**Recommendation:** Add rate limiting middleware (e.g., `next-rate-limit` or Vercel Edge Middleware).
**Status:** ⚠️ Nice to have.

---

## Performance review

### ✅ Good — Prisma client singleton
**File:** `src/lib/db.ts`
**Finding:** Uses singleton pattern (`globalThis.prisma = prisma`) to avoid connection exhaustion.
**Status:** ✅ Correct.

### ✅ Good — Excalidraw worker bundled separately
**File:** `src/app/api/excalidraw-worker/route.ts`
**Finding:** Excalidraw worker is bundled as a separate blob (patch-worker), reducing main bundle size.
**Status:** ✅ Optimized.

### 🟡 Medium — No pagination on `GET /api/boards`
**File:** `src/app/api/boards/route.ts`
**Issue:** If user has 100+ boards, the API returns all at once.
**Recommendation:** Add `?limit=20&offset=0` pagination.
**Status:** ⚠️ Needs fix for scale.

### 🟡 Medium — No caching on GET requests
**Files:** `src/app/api/boards/route.ts`, `src/app/api/boards/[id]/route.ts`
**Issue:** GET requests hit the DB every time (no Redis/cache layer).
**Recommendation:** Add `Cache-Control` headers or use SWR on client.
**Status:** ⚠️ Nice to have for high-traffic apps.

---

## Accessibility review

### 🔴 High — `role="button"` should be `role="link"`
**File:** `src/components/BoardList.tsx:82`
**Issue:** Board cards navigate to `/board/${id}` but use `role="button"`.
**Recommendation:** Use `<Link>` wrapper or `role="link"`.
**Status:** ⚠️ Same as D7 from design-review. **Fix needed.**

### 🟡 Medium — No `aria-label` on icon buttons
**File:** `src/components/BoardList.tsx:104-122` (Share/Delete buttons)
**Issue:** Share and Delete buttons have no `aria-label` (they're text-only, but still need labels for screen readers).
**Recommendation:** Add `aria-label="Share board"` and `aria-label="Delete board"`.
**Status:** ⚠️ Fix needed.

### ✅ Good — Keyboard navigation present
**File:** `src/components/BoardList.tsx:84-89`
**Finding:** `onKeyDown` handler present for Enter/Space key activation.
**Status:** ✅ Accessible.

---

## Code quality review

### ✅ Good — Consistent Typescript usage
**Files:** All `.tsx` files
**Finding:** Most functions have type annotations. `any` is used sparingly (mainly for Excalidraw API which expects `any`).
**Status:** ✅ Good.

### ✅ Good — Error boundaries present
**Files:** `src/components/ExcalidrawEditorWrapper.tsx`, `src/app/error.tsx`, `src/app/board/[id]/error.tsx`
**Finding:** Error boundaries wrap Excalidraw editor and app routes.
**Status:** ✅ Robust.

### 🟡 Medium — `console.error` in production code
**Files:** `src/auth.ts`, `src/app/error.tsx`, `src/components/ExcalidrawEditor.tsx`
**Issue:** `console.error` is kept for error logging. This is acceptable (better than silent failures), but should use a proper logging service (e.g., Sentry) in production.
**Recommendation:** Replace `console.error` with a logging service, or keep (acceptable for now).
**Status:** ⚠️ Acceptable.

### 🟢 Low — `genId()` uses `Math.random()`
**File:** `src/lib/boards.ts:3-8`
**Issue:** `Math.random()` is not cryptographically secure (but OK for non-security IDs).
**Recommendation:** Use `crypto.randomUUID()` (Node.js 19+) for better randomness.
**Status:** ⚠️ Nice to have.

---

## Deploy readiness

### ✅ Good — Build passes
**Verification:** `npm run build` succeeds (verified 2026-06-13).
**Status:** ✅ Ready.

### ✅ Good — Vercel auto-deploy configured
**Verification:** Push to `main` triggers Vercel deployment (verified).
**Status:** ✅ Ready.

### 🟡 Medium — No health check endpoint
**Issue:** Vercel can't do health checks (no `/api/health` endpoint).
**Recommendation:** Add `GET /api/health` returning `{ status: "ok" }`.
**Status:** ⚠️ Nice to have.

---

## Action items (prioritized)

| Priority | Category | Task | Est. effort |
|----------|----------|-------|-------------|
| 🔴 High | Security | Add CSRF protection to API routes | ~2h |
| 🔴 High | Accessibility | Fix `role="button"` → `role="link"` | ~15min |
| 🔴 High | Accessibility | Add `aria-label` to Share/Delete buttons | ~15min |
| 🟡 Medium | Performance | Add pagination to `GET /api/boards` | ~1h |
| 🟡 Medium | Code quality | Replace `console.error` with logging service | ~3h |
| 🟡 Medium | Deploy | Add `/api/health` endpoint | ~30min |
| 🟢 Low | Code quality | Use `crypto.randomUUID()` in `genId()` | ~15min |

**Total estimated effort:** ~7.5 hours

---

## Compliance checklist

- [x] **Typescript strict mode** — `tsconfig.json` has `strict: true` (verified)
- [x] **ESLint passes** — `npx eslint src/` returns 0 errors (verified)
- [x] **Build passes** — `npm run build` succeeds (verified)
- [ ] **CSRF protection** — missing (🔴 High)
- [ ] **Rate limiting** — missing (🟡 Medium)
- [x] **Error boundaries** — present (✅)
- [ ] **Accessibility audit** — partial (🔴 High issues found)
- [x] **Environment variables** — `.env.local` not tracked (✅)
- [x] **Prisma migration** — `schema.prisma` present (✅)

---

## Recommendation

**✅ Approve with conditions** — The app is functional and deploy-ready, but should address high-priority accessibility and security issues before public launch.

**Next steps:**
1. Fix accessibility issues (role, aria-label)
2. Add CSRF protection to API routes
3. Add pagination to boards API
4. Re-review with `/review` skill

---

*Generated by `/autoplan` v1.0.0 — gstack skill suite*
