# Design Review Report — craftisle-draw

> Auto-generated by `/design-review` skill on 2026-06-13.

---

## Summary score

| Category | Score | Notes |
|----------|-------|-------|
| Visual consistency | 7/10 | Clean Tailwind usage, but missing dark mode |
| UX polish | 6/10 | Browser confirm() dialog, no toast feedback |
| Accessibility | 7/10 | Keyboard nav present, but role misuse |
| Responsive design | 9/10 | Grid breaks correctly (1→2→3 cols) |
| Brand alignment | 8/10 | Blue theme consistent with Craftisle suite |

**Overall:** 7/10 — good foundation, needs polish on interactions and accessibility.

---

## Findings

### 🔴 High priority

#### D1 — Delete uses browser `confirm()`, not a custom modal
**File:** `src/components/BoardList.tsx:37`
**Issue:** `confirm("Are you sure...")` is browser-native, breaks visual consistency.
**Recommendation:** Replace with a custom modal or Radix Dialog.

#### D2 — Dropdown doesn't close on outside click
**File:** `src/components/Navbar.tsx:27-49`
**Issue:** Clicking outside the avatar dropdown doesn't close it (no `onClickOutside` handler).
**Recommendation:** Add `useEffect` + `document.addEventListener("click")` to close on outside click.

#### D3 — Date formatting hardcoded to `en-US`
**File:** `src/components/BoardList.tsx:95`
**Issue:** `toLocaleDateString("en-US")` ignores user locale (app targets Chinese users).
**Recommendation:** Use `undefined` (browser locale) or `zh-CN`; consider `timeAgo` format.

---

### 🟡 Medium priority

#### D4 — Avatar is small (32×32px)
**File:** `src/components/Navbar.tsx:29`
**Issue:** `w-8 h-8` is small for a user avatar.
**Recommendation:** Increase to `w-10 h-10` (40×40px).

#### D5 — No loading skeleton
**File:** `src/components/BoardList.tsx:47`
**Issue:** Shows "Loading..." text instead of a skeleton UI.
**Recommendation:** Add a skeleton grid (3-5 placeholder cards) for better perceived performance.

#### D6 — No dark mode support
**Files:** All components
**Issue:** App is light-only; user preference is "blue theme" (implies dark mode support).
**Recommendation:** Add `next-themes` + `dark:` Tailwind classes.

#### D7 — Board card uses `role="button"` instead of `role="link"`
**File:** `src/components/BoardList.tsx:82`
**Issue:** Cards navigate to `/board/${id}`, so they're links, not buttons.
**Recommendation:** Use `<Link>` wrapper or `role="link"`.

---

### 🟢 Low priority (nice to have)

#### D8 — Error state could be more visual
**File:** `src/components/BoardList.tsx:49-58`
**Issue:** Error state is text-only (no icon, no retry button).
**Recommendation:** Add an error icon + "Retry" button.

#### D9 — No toast/snackbar feedback
**File:** `src/components/BoardList.tsx:38-39`
**Issue:** Delete succeeds silently (no confirmation toast).
**Recommendation:** Add `sonner` or `react-hot-toast`.

#### D10 — Session loading flash
**File:** `src/components/Navbar.tsx:8`
**Issue:** Briefly shows "Sign In" while session loads (flash on protected pages).
**Recommendation:** Add a loading state (`const [loading, setLoading] = useState(true)`) and show a skeleton navbar.

---

## Positives ✅

1. **Consistent Tailwind usage** — no CSS-in-JS or inline styles.
2. **Responsive grid** — correct breakpoints (`sm:`, `lg:`).
3. **Keyboard accessibility** — `tabIndex`, `onKeyDown` present.
4. **Hover states** — all interactive elements have `hover:` classes.
5. **Brand color consistency** — blue-600/blue-700 used throughout.

---

## Action plan

| Priority | Task | Estimated effort |
|----------|-------|-------------------|
| 🔴 High | Replace `confirm()` with custom modal | ~1h |
| 🔴 High | Fix dropdown outside-click | ~30min |
| 🔴 High | Fix date formatting | ~15min |
| 🟡 Medium | Increase avatar size | ~5min |
| 🟡 Medium | Add loading skeleton | ~1h |
| 🟡 Medium | Add dark mode | ~3h |
| 🟡 Medium | Fix `role="button"` → `role="link"` | ~15min |
| 🟢 Low | Improve error state UI | ~30min |
| 🟢 Low | Add toast feedback | ~45min |
| 🟢 Low | Fix session loading flash | ~30min |

**Total estimated effort:** ~8.5 hours

---

## Compliance check

- [x] React hooks used correctly (useEffect, useState, useCallback)
- [x] Tailwind v3 syntax (no `@apply` abuse)
- [ ] Dark mode (missing)
- [ ] ARIA roles correct (D7 fails)
- [ ] Keyboard navigation (present, but D7 reduces score)
- [x] Responsive breakpoints correct
- [x] Loading states present (but D5 suggests improvement)
