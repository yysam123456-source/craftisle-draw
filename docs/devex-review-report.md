# DevEx Review Report — craftisle-draw

> Auto-generated by `/devex-review` skill on 2026-06-13.
> Audits developer experience: setup, documentation, tooling, friction points.

---

## DX Score

| Category | Score | Notes |
|----------|-------|-------|
| First-run setup | 6/10 | Needs Prisma + DB, env vars not documented |
| Documentation | 8/10 | `docs/developer-guide.md` exists (just generated) |
| Tooling | 7/10 | ESLint works, no pre-commit hooks |
| Friction points | 5/10 | Prisma migrate easy to forget, no seed script |
| Onboarding speed | 6/10 | ~15min to first edit (with guidance) |

**Overall:** 6.5/10 — functional but needs smoother onboarding.

---

## Findings

### 🔴 High priority (friction)

#### DX1 — No `README.md` in project root
**Issue:** New devs clone repo and see no instructions.
**Fix:** Add `README.md` with quick-start (already in `docs/developer-guide.md`, just need to copy/link).
**Effort:** ~5min.

#### DX2 — `DATABASE_URL` not documented
**File:** `.env.example` (missing)
**Issue:** New devs don't know what `DATABASE_URL` should be (SQLite? Postgres?).
**Fix:** Create `.env.example` with comments for each var.
**Effort:** ~15min.

#### DX3 — No Prisma seed script
**Issue:** New devs need test data (boards, users) to test the app.
**Fix:** Add `prisma/seed.ts` with sample data, update `package.json` with `"prisma": { "seed": "ts-node prisma/seed.ts" }`.
**Effort:** ~1h.

---

### 🟡 Medium priority (polish)

#### DX4 — No pre-commit hooks
**Issue:** Code can be committed with lint errors (ESLint not enforced).
**Fix:** Add `husky` + `lint-staged` to run ESLint on pre-commit.
**Effort:** ~30min.

#### DX5 — No `npm run dev` health check
**Issue:** Dev server starts but Prisma might fail (DB not running).
**Fix:** Add a health check endpoint (`/api/health`) and log startup status.
**Effort:** ~30min.

#### DX6 — No contribution guidelines
**Issue:** No `CONTRIBUTING.md` — unclear PR process, branch naming, etc.
**Fix:** Add `CONTRIBUTING.md` with PR checklist, branch naming (`feat/`, `fix/`, etc.).
**Effort:** ~30min.

---

### 🟢 Low priority (nice to have)

#### DX7 — No VSCode settings
**Issue:** No `.vscode/settings.json` — new devs don't get auto-format on save.
**Fix:** Add `.vscode/settings.json` with `editor.formatOnSave: true`, `eslint.validate: ["typescript", "typescriptreact"]`.
**Effort:** ~10min.

#### DX8 — No `npm run setup` script
**Issue:** New devs must manually run `npm install`, `cp .env.example .env.local`, `npx prisma migrate dev`, etc.
**Fix:** Add `"setup": "npm install && cp .env.example .env.local && npx prisma migrate dev && npm run seed"` to `package.json`.
**Effort:** ~15min.

#### DX9 — No Docker Compose for local DB
**Issue:** Devs need to install Postgres locally (or use SQLite for dev).
**Fix:** Add `docker-compose.yml` with Postgres service, update `README.md`.
**Effort:** ~1h.

---

## Positives ✅

1. **Typescript strict mode** — `tsconfig.json` has `"strict": true`.
2. **ESLint config exists** — `.eslintrc.json` with `next/core-web-vitals`.
3. **Build passes** — `npm run build` succeeds consistently.
4. **Vercel auto-deploy** — push to `main` triggers deployment.
5. **Documentation exists** — `docs/developer-guide.md` (just generated).

---

## Action plan

| Priority | Task | Est. effort | Impact |
|----------|-------|-------------|--------|
| 🔴 High | Add `README.md` | ~5min | High |
| 🔴 High | Create `.env.example` | ~15min | High |
| 🔴 High | Add Prisma seed script | ~1h | Medium |
| 🟡 Medium | Add pre-commit hooks (husky) | ~30min | Medium |
| 🟡 Medium | Add health check endpoint | ~30min | Medium |
| 🟡 Medium | Add `CONTRIBUTING.md` | ~30min | Low |
| 🟢 Low | Add VSCode settings | ~10min | Low |
| 🟢 Low | Add `npm run setup` script | ~15min | Medium |
| 🟢 Low | Add Docker Compose for local DB | ~1h | Medium |

**Total estimated effort:** ~4.5 hours

---

## Onboarding scenario (test)

**Scenario:** New dev clones `craftisle-draw` for the first time.

**Current flow (broken points highlighted):**
1. `git clone` ✅
2. `cd craftisle-draw` ✅
3. `npm install` ✅
4. `cp .env.example .env.local` ❌ (`.env.example` doesn't exist)
5. Edit `.env.local` ❌ (no comments/documentation)
6. `npx prisma migrate dev` ✅ (if Postgres is running)
7. `npm run dev` ✅ (if env vars are correct)
8. Open `http://localhost:3000` ✅ (but needs auth)

**Fixed flow (after DX improvements):**
1. `git clone` ✅
2. `cd craftisle-draw` ✅
3. `npm run setup` ✅ (runs install, copy env, migrate, seed)
4. Edit `.env.local` ✅ (comments document each var)
5. `npm run dev` ✅
6. Open `http://localhost:3000` ✅

**Time saved:** ~10-15min per new dev.

---

## Tooling audit

| Tool | Status | Notes |
|------|--------|-------|
| ESLint | ✅ Configured | `.eslintrc.json` exists |
| Prettier | ❌ Missing | No `.prettierrc` or `prettier` in ESLint config |
| Husky | ❌ Missing | No pre-commit hooks |
| Jest/Vitest | ❌ Missing | No unit test framework |
| Playwright/Cypress | ❌ Missing | No E2E tests |
| Sentry/LogRocket | ❌ Missing | No error monitoring |
| Analytics | ❌ Missing | No user analytics |

**Recommendation:** Add Prettier + Husky first (low effort, high impact).

---

## Next steps

1. **Add `README.md`** (copy from `docs/developer-guide.md`)
2. **Create `.env.example`** with documented variables
3. **Add Prisma seed script**
4. **Add Prettier + Husky**
5. **Re-run `/devex-review` to verify improvements**

---

*Generated by `/devex-review` v1.0.0 — gstack skill suite*
